info:
  name: UPDATE
  type: http
  seq: 1

http:
  method: PUT
  url: "{{url}}/usuarios/1"
  body:
    type: json
    data: |-
      {
        "nome": "Juninho1",
        "nick": "juni@",
        "email": "juninho@email.com",
        "senha": "123456"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
