info:
  name: SHOW
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/usuarios/2"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
