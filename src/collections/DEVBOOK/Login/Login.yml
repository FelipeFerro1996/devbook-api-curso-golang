info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/login"
  body:
    type: json
    data: |-
      {
        "email": "juninho@email.com",
        "senha": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
