info:
  name: STORE
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/usuarios"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
