info:
  name: STORE
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/usuarios"
  body:
    type: json
    data: |-
      {
        "nome": "Juninho",
        "nick": "juni",
        "email": "juninho@email.com",
        "senha": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
