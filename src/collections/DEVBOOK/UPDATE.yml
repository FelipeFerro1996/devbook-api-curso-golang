info:
  name: UPDATE
  type: http
  seq: 4

http:
  method: PUT
  url: "{{url}}/usuarios/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
