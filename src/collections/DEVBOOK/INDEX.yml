info:
  name: INDEX
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/usuarios"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
