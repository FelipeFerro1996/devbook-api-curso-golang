info:
  name: INDEX
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/usuarios?usuario=j"
  params:
    - name: usuario
      value: j
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
